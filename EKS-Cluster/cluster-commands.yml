#Installing AWS CLI
# curl "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o awscliv2.zip
# sudo apt install unzip -y
# unzip awscliv2.zip
# sudo ./aws/install

#Then aws configure with your credentials

#Installing kubectl
# curl -LO "https://dl.k8s.io/release/$(curl -L -s https://dl.k8s.io/release/stable.txt)/bin/linux/amd64/kubectl"
# chmod +x kubectl
# sudo mv kubectl /usr/local/bin/

#eksctl
#curl -sL https://github.com/weaveworks/eksctl/releases/latest/download/eksctl_Linux_amd64.tar.gz | tar xz -C /tmp
#sudo mv /tmp/eksctl /usr/local/bin
#eksctl version


#create eks cluster command
#eksctl create cluster --name eks-cluster-003 --region ap-south-1 --version 1.34 --nodegroup-name node-group-1 --node-type t3.small --nodes 1

#To associate OIDC provider with the EKS cluster
#eksctl utils associate-iam-oidc-provider --cluster eks-cluster-003 --region ap-south-1 --approve
#2026-01-13 17:42:13 [ℹ]  will create IAM Open ID Connect provider for cluster "eks-cluster-003" in "ap-south-1"
#2026-01-13 17:42:13 [✔]  created IAM Open ID Connect provider for cluster "eks-cluster-003" in "ap-south-1"

#To create a policy for EBS CSI Driver
#eksctl create addon --name aws-ebs-csi-driver --cluster eks-cluster-003 --region ap-south-1 --attach-policy-arn arn:aws:iam::aws:policy/service-role/AmazonEBSCSIDriverPolicy


#ONCE BOUND
#mongo-pvc   Bound    pvc-5f00af5c-4dd6-4e0b-ac7b-0cc6491552e4   10Gi       RWO            mongodb-ebs-gp3   <unset>                 6m32s








#-------------------------SCRIPT-----------------------------------#
# #!/bin/bash

# set -e  # Exit immediately if any command fails

# echo "=============================="
# echo "Updating system packages"
# echo "=============================="
# sudo apt update -y
                                                
# echo "=============================="
# echo "Installing required packages"
# echo "=============================="
# sudo apt install -y unzip curl

# echo "=============================="
# echo "Installing AWS CLI v2"
# echo "=============================="
# curl -s "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o awscliv2.zip
# unzip -q awscliv2.zip
# sudo ./aws/install

# # Cleanup
# rm -rf aws awscliv2.zip

# echo "AWS CLI version:"
# aws --version

# echo "=============================="
# echo "Installing kubectl"
# echo "=============================="
# KUBECTL_VERSION=$(curl -L -s https://dl.k8s.io/release/stable.txt)

# curl -LO "https://dl.k8s.io/release/${KUBECTL_VERSION}/bin/linux/amd64/kubectl"
# chmod +x kubectl
# sudo mv kubectl /usr/local/bin/

# echo "kubectl version:"
# kubectl version --client --output=yaml

# echo "eksctl installation"
# curl -sL https://github.com/weaveworks/eksctl/releases/latest/download/eksctl_Linux_amd64.tar.gz | tar xz -C /tmp
# sudo mv /tmp/eksctl /usr/local/bin
# eksctl version


# echo "=============================="
# echo "Installation completed successfully"
# echo "=============================="

